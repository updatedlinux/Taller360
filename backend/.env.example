NODE_ENV=development
PORT=3000

# Supabase (clave publicable / publishable — misma que en el frontend)
SUPABASE_URL=https://tu-proyecto.supabase.co
SUPABASE_KEY=tu_clave_publica

# Opcional: registro de taller vía API y script scripts/create-demo-users.js
# SUPABASE_SERVICE_ROLE_KEY=
# Contraseña compartida de los 3 usuarios demo (solo script local)
# DEMO_USER_PASSWORD=Taller360Demo!

# CORS (coma separada) o * 
# CORS_ORIGIN=http://localhost:3000
